import 'package:flutter/material.dart';

class MateScreen extends StatelessWidget {
  const MateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('친구 관련');
  }
}
